// Backend URL configuration.
// Leave empty when running as a monolith (frontend + backend on same server).
// Set to your Fly.io backend URL for split deployments:
//   window.BACKEND_URL = 'https://alpha-hive.fly.dev';
window.BACKEND_URL = '';
