PACOTE CORRIGIDO EM 2026-04-03

Principais correções aplicadas:
1. UI desbloqueada
   - Corrigido erro de sintaxe no JavaScript embutido que quebrava toda a interação.
   - Tabs, botões e atualização automática voltaram a funcionar.

2. Bootstrap da tela melhorado
   - Adicionado polling inicial para a página sair do estado zerado mais rápido.
   - Primeiro carregamento agora tenta disparar scan mesmo sem histórico anterior.

3. Backend de mercado reforçado
   - DataManager ficou mais resiliente com fallback adicional via Yahoo Finance.
   - Binance passou a usar endpoints alternativos.
   - Timeouts e cabeçalhos HTTP ficaram mais robustos.

4. Scanner otimizado
   - Coleta paralela de ativos para reduzir travamentos na montagem do snapshot.

5. Capital totalmente funcional
   - Ajustado para não sobrescrever manualmente daily_pnl e streak quando não houver trades válidos no journal.

6. Repositório pronto para GitHub
   - .gitignore atualizado para ignorar estado local, banco local e arquivos temporários.

Validações executadas:
- py_compile em todos os arquivos Python
- render da home validado localmente
- sintaxe do JavaScript validada com Node
- rotas /snapshot, /run-scan e /capital-state testadas localmente

Observação importante:
Para puxar dados reais em produção, o servidor precisa ter saída HTTP liberada. Se o host bloquear acesso externo às APIs de mercado, a UI continuará sem dados operacionais mesmo com o código corrigido.
