# IMPORTAR
from environment_transition_engine import EnvironmentTransitionEngine

# NO __init__
self.transition_engine = EnvironmentTransitionEngine()

# DENTRO DO decide (após meta_data definido)
transition = self.transition_engine.analyze_transition(meta_data)
reasons.append(f"Transição: {transition['transition_probability']} → {transition['next_environment']}")

if transition["transition_probability"] == "high":
    adjusted_score -= 0.08
    base_confidence -= 2
elif transition["transition_probability"] == "medium":
    adjusted_score -= 0.03
